import RN from 'components/RN';

export const styles = RN.StyleSheet.create({
  container: {
    borderRadius: 4,
    minHeight: 28,
    justifyContent: 'center',
  },
  contentPadding: {
    paddingVertical: 4,
  },
  outlinedContainer: {
    borderWidth: 1.5,
  },
});
